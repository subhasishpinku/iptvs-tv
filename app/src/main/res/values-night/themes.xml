<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base TV Theme - Now using MaterialComponents -->
    <style name="Theme.Leanback" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/primary_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowShowWallpaper">false</item>

        <!-- TV specific attributes -->
        <item name="android:colorBackground">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/off_white</item>
    </style>

    <!-- No ActionBar variant -->
    <style name="Theme.Leanback.NoActionBar" parent="Theme.Leanback">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Alternative: Bridge theme if you need AppCompat -->
    <style name="Theme.Leanback.Bridge" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- App Theme -->
    <style name="AppTheme" parent="Theme.Leanback">
        <item name="android:windowContentTransitions">true</item>
        <item name="browseTitleViewLayout">@layout/title_main</item>
    </style>

    <!-- TV Player Theme -->
    <style name="Theme.TvPlayer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- CircleProgressBar custom attributes -->
    <declare-styleable name="CircleProgressBar">
        <attr name="min" format="integer" />
        <attr name="max" format="integer" />
        <attr name="progress" format="integer" />
        <attr name="progressbarColor" format="color" />
        <attr name="progressBarThickness" format="dimension" />
    </declare-styleable>

</resources>